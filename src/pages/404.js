import React from 'react';

import Layout from '@common/Layout';
import Panic from '@sections/Panic';
import Footer from '@sections/Footer';

const NotFoundPage = () => (
  <Layout>
    <Panic />
    <Footer />
  </Layout>
);

export default NotFoundPage;
